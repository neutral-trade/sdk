/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findStrategyAccountPda } from "../pdas";
import { NTBUNDLE_PROGRAM_ADDRESS } from "../programs";

export const ENABLE_STRATEGY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [245, 37, 61, 122, 99, 92, 182, 30],
);

export function getEnableStrategyDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ENABLE_STRATEGY_DISCRIMINATOR,
  );
}

export type EnableStrategyInstruction<
  TProgram extends string = typeof NTBUNDLE_PROGRAM_ADDRESS,
  TAccountManager extends string | AccountMeta<string> = string,
  TAccountStrategyAccount extends string | AccountMeta<string> = string,
  TAccountBundleAccount extends string | AccountMeta<string> = string,
  TAccountReceiverAddress extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountManager extends string
        ? WritableSignerAccount<TAccountManager> &
            AccountSignerMeta<TAccountManager>
        : TAccountManager,
      TAccountStrategyAccount extends string
        ? WritableAccount<TAccountStrategyAccount>
        : TAccountStrategyAccount,
      TAccountBundleAccount extends string
        ? WritableAccount<TAccountBundleAccount>
        : TAccountBundleAccount,
      TAccountReceiverAddress extends string
        ? WritableAccount<TAccountReceiverAddress>
        : TAccountReceiverAddress,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type EnableStrategyInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type EnableStrategyInstructionDataArgs = {};

export function getEnableStrategyInstructionDataEncoder(): FixedSizeEncoder<EnableStrategyInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ENABLE_STRATEGY_DISCRIMINATOR }),
  );
}

export function getEnableStrategyInstructionDataDecoder(): FixedSizeDecoder<EnableStrategyInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getEnableStrategyInstructionDataCodec(): FixedSizeCodec<
  EnableStrategyInstructionDataArgs,
  EnableStrategyInstructionData
> {
  return combineCodec(
    getEnableStrategyInstructionDataEncoder(),
    getEnableStrategyInstructionDataDecoder(),
  );
}

export type EnableStrategyAsyncInput<
  TAccountManager extends string = string,
  TAccountStrategyAccount extends string = string,
  TAccountBundleAccount extends string = string,
  TAccountReceiverAddress extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  manager: TransactionSigner<TAccountManager>;
  strategyAccount?: Address<TAccountStrategyAccount>;
  bundleAccount: Address<TAccountBundleAccount>;
  receiverAddress: Address<TAccountReceiverAddress>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getEnableStrategyInstructionAsync<
  TAccountManager extends string,
  TAccountStrategyAccount extends string,
  TAccountBundleAccount extends string,
  TAccountReceiverAddress extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NTBUNDLE_PROGRAM_ADDRESS,
>(
  input: EnableStrategyAsyncInput<
    TAccountManager,
    TAccountStrategyAccount,
    TAccountBundleAccount,
    TAccountReceiverAddress,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  EnableStrategyInstruction<
    TProgramAddress,
    TAccountManager,
    TAccountStrategyAccount,
    TAccountBundleAccount,
    TAccountReceiverAddress,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? NTBUNDLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    manager: { value: input.manager ?? null, isWritable: true },
    strategyAccount: { value: input.strategyAccount ?? null, isWritable: true },
    bundleAccount: { value: input.bundleAccount ?? null, isWritable: true },
    receiverAddress: { value: input.receiverAddress ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.strategyAccount.value) {
    accounts.strategyAccount.value = await findStrategyAccountPda({
      receiverAddress: getAddressFromResolvedInstructionAccount(
        "receiverAddress",
        accounts.receiverAddress.value,
      ),
      bundleAccount: getAddressFromResolvedInstructionAccount(
        "bundleAccount",
        accounts.bundleAccount.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("manager", accounts.manager),
      getAccountMeta("strategyAccount", accounts.strategyAccount),
      getAccountMeta("bundleAccount", accounts.bundleAccount),
      getAccountMeta("receiverAddress", accounts.receiverAddress),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getEnableStrategyInstructionDataEncoder().encode({}),
    programAddress,
  } as EnableStrategyInstruction<
    TProgramAddress,
    TAccountManager,
    TAccountStrategyAccount,
    TAccountBundleAccount,
    TAccountReceiverAddress,
    TAccountSystemProgram
  >);
}

export type EnableStrategyInput<
  TAccountManager extends string = string,
  TAccountStrategyAccount extends string = string,
  TAccountBundleAccount extends string = string,
  TAccountReceiverAddress extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  manager: TransactionSigner<TAccountManager>;
  strategyAccount: Address<TAccountStrategyAccount>;
  bundleAccount: Address<TAccountBundleAccount>;
  receiverAddress: Address<TAccountReceiverAddress>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getEnableStrategyInstruction<
  TAccountManager extends string,
  TAccountStrategyAccount extends string,
  TAccountBundleAccount extends string,
  TAccountReceiverAddress extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NTBUNDLE_PROGRAM_ADDRESS,
>(
  input: EnableStrategyInput<
    TAccountManager,
    TAccountStrategyAccount,
    TAccountBundleAccount,
    TAccountReceiverAddress,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): EnableStrategyInstruction<
  TProgramAddress,
  TAccountManager,
  TAccountStrategyAccount,
  TAccountBundleAccount,
  TAccountReceiverAddress,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? NTBUNDLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    manager: { value: input.manager ?? null, isWritable: true },
    strategyAccount: { value: input.strategyAccount ?? null, isWritable: true },
    bundleAccount: { value: input.bundleAccount ?? null, isWritable: true },
    receiverAddress: { value: input.receiverAddress ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("manager", accounts.manager),
      getAccountMeta("strategyAccount", accounts.strategyAccount),
      getAccountMeta("bundleAccount", accounts.bundleAccount),
      getAccountMeta("receiverAddress", accounts.receiverAddress),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getEnableStrategyInstructionDataEncoder().encode({}),
    programAddress,
  } as EnableStrategyInstruction<
    TProgramAddress,
    TAccountManager,
    TAccountStrategyAccount,
    TAccountBundleAccount,
    TAccountReceiverAddress,
    TAccountSystemProgram
  >);
}

export type ParsedEnableStrategyInstruction<
  TProgram extends string = typeof NTBUNDLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    manager: TAccountMetas[0];
    strategyAccount: TAccountMetas[1];
    bundleAccount: TAccountMetas[2];
    receiverAddress: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: EnableStrategyInstructionData;
};

export function parseEnableStrategyInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedEnableStrategyInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      manager: getNextAccount(),
      strategyAccount: getNextAccount(),
      bundleAccount: getNextAccount(),
      receiverAddress: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getEnableStrategyInstructionDataDecoder().decode(instruction.data),
  };
}
