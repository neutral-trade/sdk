/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findBundleTempDataPda, findOracleDataPda } from "../pdas";
import { NTBUNDLE_PROGRAM_ADDRESS } from "../programs";

export const START_DISTRIBUTION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([118, 230, 215, 75, 83, 2, 163, 35]);

export function getStartDistributionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    START_DISTRIBUTION_DISCRIMINATOR,
  );
}

export type StartDistributionInstruction<
  TProgram extends string = typeof NTBUNDLE_PROGRAM_ADDRESS,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TAccountBundleAccount extends string | AccountMeta<string> = string,
  TAccountBundleTempData extends string | AccountMeta<string> = string,
  TAccountOracleData extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountKeeper extends string
        ? WritableSignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      TAccountBundleAccount extends string
        ? WritableAccount<TAccountBundleAccount>
        : TAccountBundleAccount,
      TAccountBundleTempData extends string
        ? WritableAccount<TAccountBundleTempData>
        : TAccountBundleTempData,
      TAccountOracleData extends string
        ? WritableAccount<TAccountOracleData>
        : TAccountOracleData,
      ...TRemainingAccounts,
    ]
  >;

export type StartDistributionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type StartDistributionInstructionDataArgs = {};

export function getStartDistributionInstructionDataEncoder(): FixedSizeEncoder<StartDistributionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: START_DISTRIBUTION_DISCRIMINATOR }),
  );
}

export function getStartDistributionInstructionDataDecoder(): FixedSizeDecoder<StartDistributionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getStartDistributionInstructionDataCodec(): FixedSizeCodec<
  StartDistributionInstructionDataArgs,
  StartDistributionInstructionData
> {
  return combineCodec(
    getStartDistributionInstructionDataEncoder(),
    getStartDistributionInstructionDataDecoder(),
  );
}

export type StartDistributionAsyncInput<
  TAccountKeeper extends string = string,
  TAccountBundleAccount extends string = string,
  TAccountBundleTempData extends string = string,
  TAccountOracleData extends string = string,
> = {
  keeper: TransactionSigner<TAccountKeeper>;
  bundleAccount: Address<TAccountBundleAccount>;
  bundleTempData?: Address<TAccountBundleTempData>;
  oracleData?: Address<TAccountOracleData>;
};

export async function getStartDistributionInstructionAsync<
  TAccountKeeper extends string,
  TAccountBundleAccount extends string,
  TAccountBundleTempData extends string,
  TAccountOracleData extends string,
  TProgramAddress extends Address = typeof NTBUNDLE_PROGRAM_ADDRESS,
>(
  input: StartDistributionAsyncInput<
    TAccountKeeper,
    TAccountBundleAccount,
    TAccountBundleTempData,
    TAccountOracleData
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  StartDistributionInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountBundleAccount,
    TAccountBundleTempData,
    TAccountOracleData
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? NTBUNDLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    keeper: { value: input.keeper ?? null, isWritable: true },
    bundleAccount: { value: input.bundleAccount ?? null, isWritable: true },
    bundleTempData: { value: input.bundleTempData ?? null, isWritable: true },
    oracleData: { value: input.oracleData ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.bundleTempData.value) {
    accounts.bundleTempData.value = await findBundleTempDataPda({
      bundleAccount: getAddressFromResolvedInstructionAccount(
        "bundleAccount",
        accounts.bundleAccount.value,
      ),
    });
  }
  if (!accounts.oracleData.value) {
    accounts.oracleData.value = await findOracleDataPda({
      bundleAccount: getAddressFromResolvedInstructionAccount(
        "bundleAccount",
        accounts.bundleAccount.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("keeper", accounts.keeper),
      getAccountMeta("bundleAccount", accounts.bundleAccount),
      getAccountMeta("bundleTempData", accounts.bundleTempData),
      getAccountMeta("oracleData", accounts.oracleData),
    ],
    data: getStartDistributionInstructionDataEncoder().encode({}),
    programAddress,
  } as StartDistributionInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountBundleAccount,
    TAccountBundleTempData,
    TAccountOracleData
  >);
}

export type StartDistributionInput<
  TAccountKeeper extends string = string,
  TAccountBundleAccount extends string = string,
  TAccountBundleTempData extends string = string,
  TAccountOracleData extends string = string,
> = {
  keeper: TransactionSigner<TAccountKeeper>;
  bundleAccount: Address<TAccountBundleAccount>;
  bundleTempData: Address<TAccountBundleTempData>;
  oracleData: Address<TAccountOracleData>;
};

export function getStartDistributionInstruction<
  TAccountKeeper extends string,
  TAccountBundleAccount extends string,
  TAccountBundleTempData extends string,
  TAccountOracleData extends string,
  TProgramAddress extends Address = typeof NTBUNDLE_PROGRAM_ADDRESS,
>(
  input: StartDistributionInput<
    TAccountKeeper,
    TAccountBundleAccount,
    TAccountBundleTempData,
    TAccountOracleData
  >,
  config?: { programAddress?: TProgramAddress },
): StartDistributionInstruction<
  TProgramAddress,
  TAccountKeeper,
  TAccountBundleAccount,
  TAccountBundleTempData,
  TAccountOracleData
> {
  // Program address.
  const programAddress = config?.programAddress ?? NTBUNDLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    keeper: { value: input.keeper ?? null, isWritable: true },
    bundleAccount: { value: input.bundleAccount ?? null, isWritable: true },
    bundleTempData: { value: input.bundleTempData ?? null, isWritable: true },
    oracleData: { value: input.oracleData ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("keeper", accounts.keeper),
      getAccountMeta("bundleAccount", accounts.bundleAccount),
      getAccountMeta("bundleTempData", accounts.bundleTempData),
      getAccountMeta("oracleData", accounts.oracleData),
    ],
    data: getStartDistributionInstructionDataEncoder().encode({}),
    programAddress,
  } as StartDistributionInstruction<
    TProgramAddress,
    TAccountKeeper,
    TAccountBundleAccount,
    TAccountBundleTempData,
    TAccountOracleData
  >);
}

export type ParsedStartDistributionInstruction<
  TProgram extends string = typeof NTBUNDLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    keeper: TAccountMetas[0];
    bundleAccount: TAccountMetas[1];
    bundleTempData: TAccountMetas[2];
    oracleData: TAccountMetas[3];
  };
  data: StartDistributionInstructionData;
};

export function parseStartDistributionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedStartDistributionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      keeper: getNextAccount(),
      bundleAccount: getNextAccount(),
      bundleTempData: getNextAccount(),
      oracleData: getNextAccount(),
    },
    data: getStartDistributionInstructionDataDecoder().decode(instruction.data),
  };
}
