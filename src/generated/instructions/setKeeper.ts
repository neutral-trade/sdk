/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { NTBUNDLE_PROGRAM_ADDRESS } from "../programs";

export const SET_KEEPER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  102, 94, 23, 78, 157, 222, 243, 214,
]);

export function getSetKeeperDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_KEEPER_DISCRIMINATOR);
}

export type SetKeeperInstruction<
  TProgram extends string = typeof NTBUNDLE_PROGRAM_ADDRESS,
  TAccountManager extends string | AccountMeta<string> = string,
  TAccountBundleAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountManager extends string
        ? WritableSignerAccount<TAccountManager> &
            AccountSignerMeta<TAccountManager>
        : TAccountManager,
      TAccountBundleAccount extends string
        ? WritableAccount<TAccountBundleAccount>
        : TAccountBundleAccount,
      ...TRemainingAccounts,
    ]
  >;

export type SetKeeperInstructionData = {
  discriminator: ReadonlyUint8Array;
  newkeeper: Address;
};

export type SetKeeperInstructionDataArgs = { newkeeper: Address };

export function getSetKeeperInstructionDataEncoder(): FixedSizeEncoder<SetKeeperInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newkeeper", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_KEEPER_DISCRIMINATOR }),
  );
}

export function getSetKeeperInstructionDataDecoder(): FixedSizeDecoder<SetKeeperInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newkeeper", getAddressDecoder()],
  ]);
}

export function getSetKeeperInstructionDataCodec(): FixedSizeCodec<
  SetKeeperInstructionDataArgs,
  SetKeeperInstructionData
> {
  return combineCodec(
    getSetKeeperInstructionDataEncoder(),
    getSetKeeperInstructionDataDecoder(),
  );
}

export type SetKeeperInput<
  TAccountManager extends string = string,
  TAccountBundleAccount extends string = string,
> = {
  manager: TransactionSigner<TAccountManager>;
  bundleAccount: Address<TAccountBundleAccount>;
  newkeeper: SetKeeperInstructionDataArgs["newkeeper"];
};

export function getSetKeeperInstruction<
  TAccountManager extends string,
  TAccountBundleAccount extends string,
  TProgramAddress extends Address = typeof NTBUNDLE_PROGRAM_ADDRESS,
>(
  input: SetKeeperInput<TAccountManager, TAccountBundleAccount>,
  config?: { programAddress?: TProgramAddress },
): SetKeeperInstruction<
  TProgramAddress,
  TAccountManager,
  TAccountBundleAccount
> {
  // Program address.
  const programAddress = config?.programAddress ?? NTBUNDLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    manager: { value: input.manager ?? null, isWritable: true },
    bundleAccount: { value: input.bundleAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("manager", accounts.manager),
      getAccountMeta("bundleAccount", accounts.bundleAccount),
    ],
    data: getSetKeeperInstructionDataEncoder().encode(
      args as SetKeeperInstructionDataArgs,
    ),
    programAddress,
  } as SetKeeperInstruction<
    TProgramAddress,
    TAccountManager,
    TAccountBundleAccount
  >);
}

export type ParsedSetKeeperInstruction<
  TProgram extends string = typeof NTBUNDLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    manager: TAccountMetas[0];
    bundleAccount: TAccountMetas[1];
  };
  data: SetKeeperInstructionData;
};

export function parseSetKeeperInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetKeeperInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { manager: getNextAccount(), bundleAccount: getNextAccount() },
    data: getSetKeeperInstructionDataDecoder().decode(instruction.data),
  };
}
